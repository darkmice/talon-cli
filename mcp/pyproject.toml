[project]
name = "talon-mcp"
version = "0.1.0"
description = "Talon 数据库 MCP Server — 让 AI 原生访问 Talon 全部 11 引擎"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0.0",
]

[project.scripts]
talon-mcp = "talon_mcp:main"
